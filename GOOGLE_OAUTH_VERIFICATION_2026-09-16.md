# Google OAuth verification — 2026-09-16

The active PromptForge Supabase project is `prompt4orge-cceab860` with ref `rupzljrpzdfrehgvbwdt`.

Google OAuth was already enabled in Supabase. The matching Google Cloud OAuth web client `Promptforge` belongs to project `fabled-imagery-360506` (project number `83605249738`). Its stale authorized redirect URI pointed to the retired Supabase project. With user confirmation, it was replaced with:

`https://rupzljrpzdfrehgvbwdt.supabase.co/auth/v1/callback`

Google Cloud confirmed **OAuth client saved**. The user then retried Google sign-in from `https://www.promptforge.com.ng/auth` and reported **signed in successfully**. This verifies the Google OAuth callback and paid-domain sign-in flow.

No client secret was copied into the project, logs, or chat.
