Analytics are added with restraint. Google Analytics records product signals such as Google sign-in completion and prompt actions, while the analytics scripts are deferred until the browser is idle where measurement requirements allow. The goal is to learn which workflows help users without making the first screen slower than necessary.

Chapter seven: optimize the production experience. Mobile zoom remains available for accessibility. The first mobile hero image is prioritized, only carefully chosen origins receive preconnect hints, and route-specific pages load in separate JavaScript chunks. The mobile install controls are deferred because they are not required to render the main Builder. Library and Blog loading states reserve space so content does not jump as data arrives. Lighthouse is used as a measurement tool, not as a substitute for judgment. After deployment, we compare performance, accessibility, Largest Contentful Paint, Total Blocking Time, and Cumulative Layout Shift on real public routes.

The result is more than a generated mockup. It is a working product with a visual identity, real member flows, protected data, connected services, a searchable content system, analytics, a deployment pipeline, and an improvement loop. That is the efficiency of building with Manus AI: start with the product intent in plain language, let the system accelerate implementation, and then keep applying the same professional cycle of inspect, build, test, deploy, and improve.

If you are demonstrating Manus to a team, focus on this loop. Begin with the clearest prompt you can write. Ask for the product, audience, constraints, and definition of done. Connect only the services the workflow needs. Test the critical journeys on the actual domain. Then use the next prompt to improve the product based on evidence. PromptForge is the example: an idea became a branded, connected, measurable web application through iterative collaboration with Manus AI.
