
Additional live verification: the authenticated browser session called `/api/trpc/admin.unlocks.list` with a read-only limit of 1 and received HTTP 200 with one existing inventory record. No code was generated or modified during this test. This confirms the owner can use the paid-domain admin API, not merely see the admin page shell.
